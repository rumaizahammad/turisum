import 'package:flutter/material.dart';

class Kasargode extends StatelessWidget {
  const Kasargode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("kazargode")),
    );
  }
}