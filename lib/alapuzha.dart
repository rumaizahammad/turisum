import 'package:flutter/material.dart';

class Alappuzha extends StatelessWidget {
  const Alappuzha({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("alappuzha")),
    );
  }
}