import 'package:flutter/material.dart';

class Palakad extends StatelessWidget {
  const Palakad({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("palakad")),
    );
  }
}