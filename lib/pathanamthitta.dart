import 'package:flutter/material.dart';

class pathanamthitta extends StatelessWidget {
  const pathanamthitta({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("pathanamthitta")),
    );
  }
}