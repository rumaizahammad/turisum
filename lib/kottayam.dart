import 'package:flutter/material.dart';

class kottayam extends StatelessWidget {
  const kottayam({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("kottayam")),
    );
  }
}