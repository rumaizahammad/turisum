import 'package:flutter/material.dart';

class idukki extends StatelessWidget {
  const idukki({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("idukki")),
    );
  }
}