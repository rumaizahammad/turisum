import 'package:flutter/material.dart';

class kollam extends StatelessWidget {
  const kollam({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("kollam"),);
  }
}