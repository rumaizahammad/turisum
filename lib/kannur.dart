import 'package:flutter/material.dart';

class kannur extends StatelessWidget {
  const kannur({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("kannur")),
    );
  }
}