import 'package:flutter/material.dart';

class Malapuram extends StatelessWidget {
  const Malapuram({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("malapuram")),
    );
  }
}