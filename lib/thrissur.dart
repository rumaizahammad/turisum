import 'package:flutter/material.dart';

class Thrissur extends StatelessWidget {
  const Thrissur({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("thrissur")),
    );
  }
}